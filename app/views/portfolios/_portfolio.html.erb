<!-- partial for data collection manual implementation -->
<p><%= link_to portfolio.title, portfolio_show_path(portfolio) %></p>
<p><%= portfolio.subtitle %></p>
<p><%= portfolio.body %></p>
<p><%= image_tag portfolio.thumb_image unless portfolio.thumb_image.nil? %></p>
<%= link_to "Edit", edit_portfolio_path(portfolio) if logged_in?(:site_admin) %>
<%= link_to 'Delete', portfolio_path(portfolio), method: :delete, data: { confirm: 'Are you sure?' } if logged_in?(:site_admin)%>